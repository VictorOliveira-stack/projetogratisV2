<h1>edit</h1>

<form action="/updatetablesprod" method="post">

    <input type="hidden" name="tablesname" value="{{tablesname}}" >
    
    <input type="hidden" name="id" value="{{data.id}}">

                <span>produto</span>
                <input type="text" name="produto" value="{{data.produto}}" >

                <span>valor</span>
                <input type="number" name="valor" value="{{data.valor}}">

                <span>quantidade</span>
                <input type="number" name="quantidade_comprada" value="{{data.quantidade_comprada}}">

                <span>Descrição</span>
                <input type="text" name="descricao" value="{{data.descricao}}" >

                <span>Estoque</span>
                <input type="number" name="estoque" value="{{data.estoque}}">
                <br> 

                <span>Quantidade Venda</span>
                <input type="number" name="quantidade_vendida" value="{{data.quantidade_vendida}}" >

                <span>Valor Venda</span>
                <input type="number" name="valor_venda" value="{{data.valor_venda}}">


                <input type="submit" value="edit" >

</form>